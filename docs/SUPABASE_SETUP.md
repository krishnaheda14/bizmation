# ARCHIVED — Supabase Setup Guide

> This document is no longer relevant. The project has migrated away from the
> Supabase client SDK. The backend uses a standard `pg` Pool connecting to any
> PostgreSQL provider. See `docs/RAILWAY_DEPLOYMENT.md` for deployment setup
> and `apps/backend/.env.example` for the required environment variables.
